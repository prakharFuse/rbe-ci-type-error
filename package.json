{
  "name": "rbe-ci-type-error",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Journey fixture: main is green; a PR whose own change breaks the typecheck is the scenario under test.",
  "scripts": { "test": "tsc --noEmit" },
  "devDependencies": { "typescript": "5.6.3" }
}
